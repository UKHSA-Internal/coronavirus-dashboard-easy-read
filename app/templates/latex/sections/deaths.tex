{% set latest = deaths.latest %}
{% set lastWeek = deaths.previous %}

\section{Deaths{% if latest.areaName != "United Kingdom" %} in {{ latest.areaName }}{% endif %}}

There {{ latest.value | pluralise("was", "were", "were") }}
{\bf{{ latest.value | format_number }}} death{{ latest.value | pluralise("", "s", "s") }}
within 28 days of a positive test for coronavirus reported on {{ latest.formatted }}.


Between {{ lastWeek.formatted }} and {{ latest.formatted }}, there have
been {\bf{{ latest.rollingSum | format_number }}} deaths within 28 days of a positive coronavirus test.
This shows {{ latest.change | comparison_verb("an increase", "a decrease", "no change" ) }}
{%- if latest.change != 0 %}
	of {\bf{{ latest.percentage | format_number }}\%}
{% endif -%}
compared to the previous 7 days.

